apply plugin: 'java'
apply plugin: 'application'

mainClassName = "au.com.rea.robot.RobotSimulation"

repositories {
    jcenter()
}

run {
    standardInput = System.in
}

dependencies {
    compile 'org.slf4j:slf4j-api:1.7.21'
    testCompile 'junit:junit:4.12'
}
